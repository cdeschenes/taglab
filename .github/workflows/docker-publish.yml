name: Publish Docker Image

on:
  push:
    branches:
      - master
      - beta

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag
        id: tag
        run: |
          if [ "${{ github.ref_name }}" = "master" ]; then
            echo "tag=latest" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Build
        run: docker build -t ghcr.io/cdeschenes/taglab:${{ steps.tag.outputs.tag }} .

      - name: Push
        run: docker push ghcr.io/cdeschenes/taglab:${{ steps.tag.outputs.tag }}
